<?php

/**
 * SECURITY: This controller has been disabled.
 * The fixAdminRole() endpoint allowed any authenticated user to self-promote to admin.
 * Use the Artisan command `php artisan make:admin {email}` instead for admin provisioning.
 */

namespace App\Http\Controllers;

class DebugAuthController extends Controller
{
    // All debug endpoints removed — see MakeUserAdmin artisan command.
}
